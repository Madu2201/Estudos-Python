# Cria uma matriz (lista de listas) chamada 'dados'
dados = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]

# Percorre cada linha da matriz
for linha in dados:
    # Para cada linha, percorre cada elemento (coluna)
    for coluna in linha:
        # Imprime o valor da coluna atual
        print(coluna)
